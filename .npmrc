# The only peer dependency is `astro`, which the host site provides. The standalone
# dev harness is Vite + React and does not need it, so do not pull ~200 packages of
# Astro into a local install. This also keeps package-lock.json in sync, which plain
# `npm ci` requires.
legacy-peer-deps=true
